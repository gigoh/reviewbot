# VCS Configuration
# Platform is automatically detected from the URL
# Configure one or both tokens depending on which platforms you use

# GitLab Configuration (required for GitLab MRs)
GITLAB_URL=https://gitlab.com
GITLAB_TOKEN=your-gitlab-personal-access-token

# GitHub Configuration (required for GitHub PRs)
GITHUB_TOKEN=your-github-personal-access-token

# LLM Configuration
# Choose provider: 'anthropic' or 'ollama' (default: anthropic)
LLM_PROVIDER=anthropic

# Anthropic Configuration (required if LLM_PROVIDER=anthropic)
ANTHROPIC_API_KEY=your-anthropic-api-key

# Ollama Configuration (required if LLM_PROVIDER=ollama)
# OLLAMA_ENDPOINT=http://localhost:11434
# OLLAMA_MODEL=gemma3:4b

# Review Settings (optional)
# Review language: 'english' (default), 'korean', 'japanese', 'chinese'
# - english: Reviews only in English
# - korean/japanese/chinese: Bilingual reviews (selected language + English)
REVIEW_LANGUAGE=english

MAX_DIFF_SIZE=50000
REVIEW_PROMPT_TEMPLATE=default
